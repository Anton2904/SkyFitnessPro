import { useNavigate } from "react-router-dom"; import type { Workout, WorkoutProgress } from "../../types";
export function WorkoutPicker({courseId,workouts,progress,onClose}:{courseId:string;workouts:Workout[];progress:WorkoutProgress[];onClose:()=>void}){const navigate=useNavigate();return <div><h2>Выберите тренировку</h2><div className="workoutList">{workouts.map((w)=>{const done=progress.find((p)=>p.workoutId===w._id)?.workoutCompleted;return <button key={w._id} onClick={()=>{onClose();navigate(`/workout/${courseId}/${w._id}`)}}><span>{w.name}</span><b className={done?"done":""}>{done?"✓":"→"}</b></button>})}</div></div>}
